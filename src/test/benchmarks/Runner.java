package test.benchmarks;

/**
 * Runs a specified BenchmarkCase on a specified network protocol.
 *
 * The protocol is defined by two functions: createBroadcaster() and createClient(index).
 */
public class Runner {
}
